This directory is reserved for future shared AgentX contracts and abstractions.

The `modules/gemini_live_agent` service added for the Gemini Live Agent Challenge is intentionally decoupled from this folder so it can run as an independent plugin/service.